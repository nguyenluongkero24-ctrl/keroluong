package com.example.data.model

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "monthly_overrides")
data class MonthlyOverride(
    @PrimaryKey val monthStr: String, // format: "yyyy-MM"
    val overriddenAttendanceBonus: Double
)
